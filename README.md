# logs-analyzer
Allows to diagnose the most common errors from Minecraft logs. Use the from_url variable to toggle between fetching from a local file and fetching from a discord-paste.curseforge.com URL.

## Additions compared to Curseforge bot
- Indicating which config file is corrupted
- Detecting more mod version errors
- Indicating which mods have been compiled to wrong Java version
